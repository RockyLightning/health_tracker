<%= form_for(@cal) do |f| %>
  <% if @cal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cal.errors.count, "error") %> prohibited this step from being saved:</h2>

      <ul>
      <% @cal.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :cals_consumed %><br>
    <%= f.number_field :cals_consumed %>
  </div>
  <div class="field">
    <%= f.label :date %><br>
    <%= f.date_select :date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
